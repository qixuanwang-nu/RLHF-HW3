# Environment variables for RLHF Part 4 evaluation (GPT-4.1 Mini as judge).
#
# This repo environment blocks creating ".env" files, so we use "env.example".
# You can optionally copy this to ".env" in your own environment if desired.
#
# Usage (bash/zsh):
#   set -a
#   source env.example
#   set +a
#
# Or pass it explicitly:
#   python evaluate_models.py --env_file env.example ...
#
# OpenAI API
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# Judge model (requested)
OPENAI_MODEL=gpt-4.1-mini

# Optional: keep evaluation runs deterministic
PYTHONHASHSEED=0

